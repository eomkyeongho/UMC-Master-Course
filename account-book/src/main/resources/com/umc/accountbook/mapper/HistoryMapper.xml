<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.umc.accountbook.mapper.HistoryMapper">
    <select id="getHistory" resultType="com.umc.accountbook.domain.History">
        SELECT *
        FROM HISTORY
        WHERE USER_ID=#{user_id} and DATE=#{date}
    </select>

    <select id="getTodaySuccess" parameterType="String" resultType="com.umc.accountbook.domain.History">
        select * from
            history
        where user_id = #{user_id}
          and date = #{date}
          and history.DAILY_AVAIL_AMOUNT - history.SPENDING <![CDATA[<]]> 0
    </select>

    <select id="getHistoryList" parameterType="int" resultType="map">
        select history.history_id, history.date, (CASE WHEN history.MAX_AVAIL_AMOUNT >= history.SPENDING THEN 1 ELSE 0 END) AS success, goal.rest_amount - history.spending as rest_amount
        from history history
        right join goal goal
        on history.user_id = goal.user_id
        where history.user_id = 3
        order by history.date
    </select>
</mapper>